Basic input forms may allow insertion of tags. This can lead to XSS with `<script>` tags.

If iFrames are used on the website, use [[Dirbuster]] to discover hidden files, then the `src` of the iframe can be set to that file to view it.