Response from the back-end request is not included in the front-end response.

## DETECTION
Try using SSRF techniques but to a server you control.

**Note**: The initial DNS lookup might be visible without any actual HTTP request. This is because DNS is almost fully allowed by servers ( required for functionality ), but network-level security blocks unexpected HTTP requests.

Exploring the server infrastructure is not possible as no response is received. But probing the systems and sending malicious payloads to randomly check for other vulnerabilities is still possible.

If the connection to attacker server is possible, then the server can be configured to send malicious HTTP back.

Might even lead to remote code execution on the server.