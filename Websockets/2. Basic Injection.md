User input may be just inserted as such ( leads to XSS ).
Just make sure non-encoded data reaches the server ( intercept packet, change URL encoded params if needed )


Bonus: alert\`1\` instead of alert(1)